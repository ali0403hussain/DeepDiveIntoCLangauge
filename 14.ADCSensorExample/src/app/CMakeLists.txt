set(EXE_NAME "ADCSensorExample")
set(SRC_FILE main.c)
set(INC_PATH "../../inc" "../lib/adc" "../lib/sensor" "../lib/time")
add_executable(${EXE_NAME} ${SRC_FILE})
target_link_libraries(${EXE_NAME} ${LIB_SENSOR})
target_compile_options(${EXE_NAME} PRIVATE -Werror -Wall -Wextra)